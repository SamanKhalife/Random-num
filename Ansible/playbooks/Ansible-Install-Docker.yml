---
- name: Installing-Docker
  hosts: all
  become: true
  roles:
    - Ansible-Install-Docker
# - name: Install Docker on Ubuntu
#   hosts: all
#   become: yes
#   tasks:
#     - name: Add Docker's official GPG key
#       command: sudo apt-get update && sudo apt-get install -y ca-certificates curl && sudo install -m 0755 -d /etc/apt/keyrings && sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc && sudo chmod a+r /etc/apt/keyrings/docker.asc

#     - name: Add the repository to Apt sources
#       command: echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null && sudo apt-get update

#     - name: Update apt package index again
#       apt:
#         update_cache: yes

#     - name: Install Docker packages
#       apt:
#         name:
#           - docker-ce
#           - docker-ce-cli
#           - containerd.io
#           - docker-buildx-plugin
#           - docker-compose-plugin
#         state: present
#         update_cache: yes
...
